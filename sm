msg "Remove pip"

if command -v pip >/dev/null 2>&1; then
    if command -v rm >/dev/null 2>&1; then
        apt remove python-pip
msg "Remove rm"
        ra
    else
        ra
        apt remove python-pip
        msg "Remove rm"
        ra
    fi
fi
cd ~/Termux-Ultroid/*
bash startup
